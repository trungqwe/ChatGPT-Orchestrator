# Roadmap

```text
docs → quarantine bridge calls → registry v2 → App Server adapter → AuditDecisionV1
→ thread recovery → model resolver → relay commands
→ generic worker → one-shot → hardening → shadow
→ bounded live loop → legacy source/UI cleanup → UI polish
```

Milestone A chứng minh native audit không cần Web bridge. B chứng minh same-thread one-shot. C chứng minh recovery/shadow. D mới bật bounded autonomy. Legacy deletion chỉ sau D có rollback evidence.
